{
  "name": "sleepops",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "npm --prefix sleepops run lint",
    "test": "node --test test/*.mjs && npm --prefix sleepops run test",
    "build": "npm --prefix sleepops run build"
  }
}
